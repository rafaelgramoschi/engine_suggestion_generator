A simple tool which helps me scrape keywords from Search engines' API (qwant, duckduckGo, Google & Amazon).

It should function somewhat like KeywordShitter.com (which only works for Google searches).
